import Sprite from "./Sprite";
export default class AnimSprite extends Sprite {
    constructor(engine, name, image) {
        super(engine, name, image);
    }
    update(utime) {
        utime;
    }
}
